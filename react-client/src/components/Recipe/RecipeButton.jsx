/*
This button should be able to display custom text passed in and functionality that
opens the recipe details form, passing whichever recipe id it has
*/
import propTypes from 'prop-types';

const RecipeButton = ( {buttonText, buttonFunction} ) => {
    

    return (
        <>
        </>
    );
}

export default RecipeButton;